---
title: "Установка"
description: "Установка и настройка @ambrosia-unce/websocket"
---

# Установка

## Зависимости

```bash
bun add @ambrosia-unce/websocket @ambrosia-unce/websocket-elysia
```

`@ambrosia-unce/websocket` — ядро (декораторы, интерфейсы, тестирование). `@ambrosia-unce/websocket-elysia` — адаптер для Elysia.

## Настройка с Elysia

```typescript title="main.ts"
import "reflect-metadata";
import { Elysia } from "elysia";
import { HttpApplication } from "@ambrosia-unce/http";
import { ElysiaProvider } from "@ambrosia-unce/http-elysia";
import { WsApplication } from "@ambrosia-unce/websocket";
import { ElysiaWsProvider } from "@ambrosia-unce/websocket-elysia";
import { ChatGateway } from "./chat.gateway";

// 1. Создаём HTTP приложение
const app = await HttpApplication.create({
  provider: ElysiaProvider,
  controllers: [...],
});

// 2. Подключаем WebSocket к тому же Elysia инстансу
const elysia = app.getNativeApp();
const wsProvider = new ElysiaWsProvider(elysia);

const wsApp = await WsApplication.create({
  provider: wsProvider,
  container: app.getContainer(), // Общий DI контейнер
  gateways: [ChatGateway],
});

// 3. Запускаем
await app.listen(3000);
```

## Структура проекта

```
src/
├── main.ts
├── gateways/
│   ├── chat.gateway.ts
│   └── notification.gateway.ts
├── services/
│   └── chat.service.ts
└── packs/
    └── chat.pack.ts
```

## Следующие шаги

- [Quick Start — первый гейтвей](/docs/websocket/getting-started/quick-start)

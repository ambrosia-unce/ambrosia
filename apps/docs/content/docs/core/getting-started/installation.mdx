---
title: "Installation"
description: "Install and configure @ambrosia-unce/core"
---

# Installation

Install @ambrosia-unce/core using your preferred package manager.

## Requirements

- Bun 1.0+
- TypeScript 5.0+

## Install

```bash
bun add @ambrosia-unce/core
```

## TypeScript Configuration

Enable decorators in `tsconfig.json`:

```json
{
  "compilerOptions": {
    "experimentalDecorators": true,
    "emitDecoratorMetadata": true
  }
}
```

## Verify Installation

```typescript
import { Container, Injectable } from '@ambrosia-unce/core';

@Injectable()
class TestService {}

const container = new Container();
const service = container.resolve(TestService);

console.log('Installation successful!');
```

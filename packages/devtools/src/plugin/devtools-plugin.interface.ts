/**
 * Re-export plugin interfaces from types for convenient importing.
 *
 * The canonical definitions live in ../types.ts.
 */

export type {
  DevToolsPlugin,
  DevToolsRouter,
  DevToolsEventEmitter,
  TabDefinition,
  PluginRouteHandler,
} from "../types.ts";
